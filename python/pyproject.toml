[project]
name = "python-app"
version = "0.1.0"
description = "Example Python app in multi-language repo"
requires-python = ">=3.8"
dependencies = [
  "requests>=2.0"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
